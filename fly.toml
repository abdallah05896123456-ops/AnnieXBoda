app = "anniexboda-5ncltw"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

# ⛔️ شيلنا أمر التشغيل عشان ياخده من الـ Dockerfile
# [processes]
#   app = "python3 -m AnnieXMedia"

# ⛔️ ملغي عشان يشتغل Worker بدون بورت
# [http_service]
#   internal_port = 8080
#   force_https = true
#   auto_stop_machines = false
#   auto_start_machines = true
#   min_machines_running = 1
#   processes = ["app"]

# 🔥 المواصفات (16 CPU / 90GB RAM) 🔥
[[vm]]
  cpu_kind = "performance"
  cpus = 16
  memory_mb = 92160
