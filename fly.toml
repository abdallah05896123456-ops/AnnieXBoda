app = "anniexboda-ongrzg"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

# [env]  <-- علقنا ده مش محتاجينه
#   PORT = "8080"

# ⛔️ علقنا (وقفنا) كل ده عشان السيرفر ميبصش على البورت
# [http_service]
#   internal_port = 8080
#   force_https = true
#   auto_stop_machines = false
#   auto_start_machines = true
#   min_machines_running = 1
#   processes = ["app"]

# أمر التشغيل (مهم جداً نسيبه)
[processes]
  app = "python3 -m AnnieXMedia"

# مواصفات السيرفر
[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
